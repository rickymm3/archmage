-#Add new attribute to User_notifications(where show_to_user:true)
-uns = current_user.user_notifications.where(viewed:false)
-if uns.present?
  .alert.alert-warning.alert-dismissible{:role => "alert"}
    %button.close{"data-dismiss" => "alert", :type => "button"}
      %span{"aria-hidden" => "true"}×
      %span.sr-only Close
    -uns.each do |un|
      .row
        = UserNotification.render_message(un, current_user)